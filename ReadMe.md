# Turtle Generative Art

## Running a script

__Note__: this repo is meant to go inside a larger folder containing also a `Drawing` folder.

Scripts meant to be run can be found under `paper`. They should be run from the main folder with:

`python -m paper.basic_paper`